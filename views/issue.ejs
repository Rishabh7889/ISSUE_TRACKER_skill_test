<!DOCTYPE html>
<html>
  <head>
    <title>Issue: <%= issue.title %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <div class="issue">
        <h1><%= issue.title %></h1>
        <p>
          Status:
          <span class="status <%= issue.status.toLowerCase() %>"
            ><%= issue.status %></span
          >
        </p>
        <p>Description: <%= issue.description %></p>

        <% if (issue.status === 'Open') { %>
        <form action="/issues/<%= issue.id %>/close" method="post">
          <input type="submit" value="Close Issue" />
        </form>
        <% } else { %>
        <p>This issue is already closed.</p>
        <% } %>

        <a href="/">Back to Issues</a>
      </div>
    </div>
  </body>
</html>
